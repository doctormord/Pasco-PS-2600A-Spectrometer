# Runtime dependencies for the PASCO PS-2600A Spectrum Analyzer GUI.
PyQt6>=6.5
pyqtgraph>=0.13
numpy>=1.24
scipy>=1.10

# Optional — IES TM-30-18 (Rf/Rg, 16 hue bins, colour-vector graphic, 99-sample
# fidelity) and CQS (Qa/Qf/Qg) in the colour tab's "TM-30 / CQS" sub-tab and the
# colour report. Validated against the official IES calculator. Without it those
# metrics simply don't appear; CRI / CIE / everything else is unaffected.
colour-science>=0.4.4

# Optional — CIE 1931 chromaticity diagram fill, and the filter-report export
# (plot + metrics table as PDF/PNG) in both the desktop and web apps. Without it
# the desktop falls back to a pyqtgraph PNG + CSV; the web /api/filter/report
# endpoint returns HTTP 503.
matplotlib>=3.7

# Optional — Ocean Optics / Ocean Insight HDX-UV-VIS (raw USB / OBP protocol)
# The HDX driver (_device_ocean.py) talks to the device directly over USB
# using the Ocean Binary Protocol — it does NOT use seabreeze.
# pip install pyusb libusb-package
# libusb-package bundles the libusb backend, so no system libusb is required.
# On Linux a udev rule for VID 2457 / PID 2003 may be needed for non-root access.
# pyusb>=1.2
# libusb-package>=1.0

# Optional — ASEQ Instruments / Lasertrack LR-2T (HID via hidapi)
# pip install hid
# On Windows: the device uses the default HID driver — no Zadig needed.
# On Linux: run once:  sudo cp udev/99-pasco-ps2600a.rules /etc/udev/rules.d/
#           then add a matching rule for VID_E220 PID_0100 if not present.
# hid>=1.0.4

# --- Web GUI (web_server.py) ---
# FastAPI server + uvicorn ASGI server, accessed from a browser on the same WiFi.
# 'websockets' is REQUIRED for the live spectrum stream (/ws/stream); without it
# uvicorn logs "Unsupported upgrade request" and the browser never connects.
fastapi>=0.110
uvicorn>=0.27
websockets>=12.0
